[project]
name = "macpkg-migrate"
version = "0.3.0"
requires-python = ">=3.9"
[project.scripts]
macpkg-migrate = "macpkg_migrate.cli:main"
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
