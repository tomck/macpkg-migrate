[project]
name = "macpkg-migrate"
version = "0.5.0"
requires-python = ">=3.9"
dependencies = ["macpkg-migrate-core>=0.5,<0.6"]
[project.scripts]
macpkg-migrate = "macpkg_migrate.cli:main"
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
